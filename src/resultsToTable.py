#!/usr/bin/env python

"""
Convert the JSON from ectyper to table format
"""

import logging

log = logging.getLogger(__name__)

def results_dict_to_table(results_dict)
    """
    Take the results dict, and converts it to output in table, structure
    :param results_dict: 
    :return: results_as_table
    """

